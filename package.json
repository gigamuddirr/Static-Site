{
  "name": "static-site",
  "version": "1.0.0",
  "description": "A static site generator",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=development node scripts/build.js && npx serve public",
    "build": "NODE_ENV=production node scripts/build.js",
    "serve": "NODE_ENV=development node scripts/build.js && npx serve docs -s",
    "build:prod": "NODE_ENV=production node scripts/build.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^11.2.0",
    "gray-matter": "^4.0.3",
    "marked": "^11.1.1"
  },
  "devDependencies": {
    "canvas": "^2.11.2",
    "serve": "^14.2.1"
  }
}
